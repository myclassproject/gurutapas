<%= form_for(@bookevent) do |f| %>
  <% if @bookevent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookevent.errors.count, "error") %> prohibited this bookevent from being saved:</h2>

      <ul>
      <% @bookevent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :start_at %><br />
    <%= f.datetime_select :start_at %>
  </div>
  <div class="field">
    <%= f.label :end_at %><br />
    <%= f.datetime_select :end_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
